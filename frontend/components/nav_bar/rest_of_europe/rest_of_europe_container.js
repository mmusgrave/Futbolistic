import { connect } from 'react-redux';
import RestOfEurope from './rest_of_europe';


const mapStateToProps = () => {
  return {
  }
};

const mapDispatchToProps = () => {
  return {
  };

};

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(RestOfEurope);
